<ul>
  <li>
    <% @spots.each do |spot|%>
      <%= link_to spot.name, spot_path(spot.id)%>
      <%= image_tag spot.spot_images[0].src.url, width: "300px", height:"300px"%>
    <% end%>
  </li>
</ul>